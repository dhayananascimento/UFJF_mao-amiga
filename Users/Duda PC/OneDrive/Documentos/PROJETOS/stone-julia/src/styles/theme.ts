import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#1D1729',
    },
    secondary: {
      main: '#186cda',
      light: '#00A1E9',
    },
  },
  typography: {
    fontFamily: 'var(--font-ample-soft)',
  },
});
